# Curriculum Index QA Notes - 2026-04-16

These notes capture user QA feedback for the next taxonomy and hierarchy pass.

## Status

- Do not implement these changes yet unless explicitly requested in a future session.
- These are saved as a working checklist for the next Index cleanup pass.

## Drills / Constraint-Led Games

The following items should be removed from `category: Drills`:

- `Guard Retention Rounds`
- `Positional Isolation Rounds`
- `Constraint-Led Games`
- `Positional Sparring Systems`
- `Hand-Fight Specific Rounds`
- `Passing Decision Tree Rounds`
- `Submission Chain Rounds`

User note:

- These are more tied to games and should fit under a category closer to `Constraint-Led Games`, not `Drills`.

## Escapes

- Remove `Upa Escape` because it is the same as `Trap And Roll`.
- `Inversion Recovery`
  - Add related position: `Open Guard`
  - Remove related position: `Side Control`
- `Hide The Choking Side`
  - This is more conceptual than an actual back escape.
- `Shoulder Slide Escape`
  - Keep only if wording is changed to be more concrete as an actual escape.
  - Suggested direction:
    - Use shoulder positioning and careful rotation to turn into closed guard while escaping the back.

## Turtle Escapes

- Change `Roll Escape` to `Granby Roll`
- Change `Switch` to `Hip Switch`
- Add `Sit-Out`
  - Example use case:
    - when a training partner gets a chest wrap on you
    - sit out to one side
    - break their grips apart
    - use that to escape

Related note:

- `Sit-Out` already exists in `Movements`, but it should also appear in `Escapes` for turtle-specific application.

## Submission Defense / Escapes

- `Triangle Posture Escape`
  - Update description to:
    - `A triangle escape focused on rebuilding posture and breaking the legs apart`
- Rename:
  - `Triangle Hand Fight And Angle-Kill Escape`
  - to:
    - `Triangle Escape With Hand Fighting/Angles`
- Update description to:
  - `A triangle defense that hand fights the setup picking a side with your elbow to cut an angle to separate the legs/make triangle harder to lock.`

## Passing

- Remove `Knee Slice Passing`
  - `Knee Cut` as a specific pass is enough.
- Remove `Torreando Passing` as a passing style
  - It is really an example of `Outside Passing`
  - Keep it only as a specific pass / technique
  - Also spelling needs cleanup (`Toreando` / `Torreando` consistency review)
- Add / rename passing style:
  - `Stack Pass Style`
- Description suggestion:
  - `A pass that compresses the lower body, folds the hips and uses posture control to break/clear the guard.`
- Remove `Throw Leg Pass`
  - It is effectively the same as `Throw-By Pass`
- Rename:
  - `Reverse Half Pass`
  - to:
    - `Reverse Half Guard Pass`

## Guard Retention

- Category is still very unfinished.
- More items need to be added later.

## Leg Locks

Need to manually add later:

- `Mikey Lock`
- `Abe Lock`
- `Junny Lock`
- `Lachy Lock`

Also split:

- `Heel Hook`
  - into:
    - `Outside Heel Hook`
    - `Inside Heel Hook`

## Movements

- Some movements used in guard retention still need to be added later.
- User is not sure whether all seed expansion is fully complete yet.
- `Sit-Out` already exists in `Movements`, but it should also be represented in turtle escapes as noted above.

## Pins And Control

- This category still needs substantial expansion later.

## Positional Sparring

- This category also still needs more items later.

## Positions

- Remove `Neutral Standing`
- `Wrestling Stance` and `Judo Stance`
  - are still standing positions
  - but may fit better elsewhere, possibly under strategy or connected more directly with takedowns
- Remove `Over-Under Passing Position`
  - This is not really a standalone position
  - It is more of a passing style / passing exchange
- Possible alternative mentioned:
  - `Double Unders (Double Under the legs)`
  - but even that likely belongs more under passing than positions
- More positions are still missing and need to be added later.

## Self-Defense Basics

- QA review stopped at `Self-Defense Basics`
- Remaining categories still need review in the next session

## Remaining QA To Finish Later

User plans to continue QA on the last 7 categories after returning.

## Next Session Suggested Order

1. Continue QA from `Self-Defense Basics` onward
2. Finish category-by-category review
3. Apply taxonomy and naming fixes from this note
4. Revisit unfinished categories:
   - `Guard Retention`
   - `Leg Locks`
   - `Movements`
   - `Pins And Control`
   - `Positional Sparring`
   - `Positions`
